apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kyverno-preview-cosign-public-key-reader
  namespace: argo
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    # Candidate access is dormant outside the bounded overlap window; it avoids
    # broad Secret enumeration while permitting zero-downtime trust rotation.
    resourceNames: ["preview-cosign-public", "preview-cosign-public-candidate"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kyverno-preview-cosign-public-key-reader
  namespace: argo
subjects:
  - kind: ServiceAccount
    name: kyverno-admission-controller
    namespace: kyverno
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kyverno-preview-cosign-public-key-reader
