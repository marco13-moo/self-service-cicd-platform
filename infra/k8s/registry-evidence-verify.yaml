apiVersion: batch/v1
kind: Job
metadata:
  name: registry-evidence-verify
  namespace: argo
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      volumes:
        - name: public-key
          secret:
            secretName: preview-cosign-public-conformance
      containers:
        - name: signature
          image: ghcr.io/sigstore/cosign/cosign:v2.6.4
          command: [cosign]
          args:
            - verify
            - --key
            - /cosign/cosign.pub
            - --insecure-ignore-tlog
            - --allow-http-registry=true
            - --allow-insecure-registry=true
            - EVIDENCE_TEST_IMAGE
          volumeMounts:
            - name: public-key
              mountPath: /cosign
              readOnly: true
---
apiVersion: batch/v1
kind: Job
metadata:
  name: registry-evidence-verify-attestation
  namespace: argo
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      volumes:
        - name: public-key
          secret:
            secretName: preview-cosign-public-conformance
      containers:
        - name: policy-attestation
          image: ghcr.io/sigstore/cosign/cosign:v2.6.4
          command: [cosign]
          args:
            - verify-attestation
            - --key
            - /cosign/cosign.pub
            - --type
            - https://self-service-cicd.dev/attestations/vulnerability-policy/v1
            - --insecure-ignore-tlog
            - --allow-http-registry=true
            - --allow-insecure-registry=true
            - EVIDENCE_TEST_IMAGE
          volumeMounts:
            - name: public-key
              mountPath: /cosign
              readOnly: true
---
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: registry-evidence-recovery-conformance
spec:
  background: false
  rules:
    - name: verify-recovered-evidence
      match:
        any:
          - resources:
              kinds: [Pod]
              namespaceSelector:
                matchLabels:
                  platform.evidence-recovery-conformance: enabled
      verifyImages:
        - imageReferences: ["EVIDENCE_REGISTRY_PATTERN"]
          failureAction: Enforce
          mutateDigest: false
          required: true
          verifyDigest: true
          imageRegistryCredentials:
            allowInsecureRegistry: true
          attestors:
            - entries:
                - keys:
                    publicKeys: k8s://argo/preview-cosign-public-conformance
                    rekor:
                      ignoreTlog: true
